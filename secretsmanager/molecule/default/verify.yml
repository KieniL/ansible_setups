---
# This is an example playbook to execute Ansible tests.

- name: Verify
  hosts: all
  gather_facts: false
  tasks:
  - name: include verify tasks
    include_tasks: ./verify/verify.yml
    loop: ['consul', 'vault']
